<% if (errors && errors.length > 0) { %>
        <% errors.forEach((error) => { %>
            <div class="alert alert-danger" role="alert">
                <%= error %>
            </div>
        <% }); %>
<% } %>